import React from 'react';
import { Button, TimelineEditor } from '@/components';

const Page: React.FC = () => {
	return (
		<main>
			<Button>Click Me</Button>
			<TimelineEditor />
		</main>
	);
};

export default Page;
