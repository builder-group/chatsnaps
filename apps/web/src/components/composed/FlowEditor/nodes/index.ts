import { type TDefaultNodeMap } from '../types';
import { DefaultNode } from './DefaultNode';

export * from './DefaultNode';

export const defaultNodeMap: TDefaultNodeMap = {
	default: DefaultNode
};
